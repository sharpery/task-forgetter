
<h4>New Task:</h4>
<div class='new-item'>
  <%= form_for Item.new, :remote => true do |f| %>
    <%= f.label :title %>
    <%= f.text_field :title %>
    <%= f.submit "Save", class: 'btn btn-success' %>
  <% end %>
</div>
