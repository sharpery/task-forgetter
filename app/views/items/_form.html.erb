<h3>New Task</h3>
<div class='new-item'>
  <%= form_for [item], remote: true do |f| %>
    <%= f.label :title %>
    <%= f.text_field :title %>
    <%= f.submit "Save", class: 'btn btn-success' %>
  <% end %>
</div>
